import { DefaultTheme } from "styled-components";

export const theme: DefaultTheme = {
  fonts: {
    basic: "'Rubik', sans-serif;",
  },
  palette: {
    heavyMetal: "hsl(0, 0%, 17%)",
    mountainMist: "hsl(0, 0%, 59%)",
  },
};

export default theme;
